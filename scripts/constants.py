#!usr/bin/env python2

GROUND_THRESHOLD = 0.1  # meters
MAX_CLUSTER_PTS = 200  # max num of pts in cluster

PADDING_X = 0.1  # bbox padding for clustering, x-coord of bbox
PADDING_Y = 0.1  # bbox padding for clustering, y-coord of bbox
PADDING_Z = 0.1  # bbox padding for clustering, z-coord of bbox
